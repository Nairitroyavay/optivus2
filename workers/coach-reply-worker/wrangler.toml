name = "optivus-coach-reply"
main = "src/index.js"
compatibility_date = "2026-05-08"

[vars]
GEMINI_MODEL = "gemini-2.5-flash"

# Store runtime secrets with Wrangler:
# wrangler secret put GEMINI_API_KEY
# wrangler secret put FIREBASE_PROJECT_ID
# Optional per-user preview rate limit:
# wrangler secret put COACH_REPLY_RATE_LIMIT_PER_MINUTE
