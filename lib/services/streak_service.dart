// lib/services/streak_service.dart
//
// Placeholder — full implementation in Task 4.1.
// Exists now so EventOrchestrator can hold a typed reference.

import 'package:flutter/foundation.dart';

class StreakService {
  /// Will eventually iterate active habits and roll up streak counts.
  Future<void> runDayCloseRollup(String date) async {
    debugPrint('[StreakService] runDayCloseRollup($date) — not yet implemented');
  }
}
